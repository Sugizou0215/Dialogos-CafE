<section id="event" class="bg-light">
  <div class="container">
    <div class='row'>
      <%= render 'users/user_info',user: @user %>
      <div class='col-9 mt-2'>
        <h2>タグ:<%= @tag.name %>検索結果一覧</h2>
        <div class="col-12 mt-2">
          <%= render 'events/index',events: @events %>
        </div>
      </div>
    </div>
  </div>
</section>